export interface Kline {
  time: number; // Open time as a number (timestamp)
  open: number; // Open price
  high: number; // High price
  low: number; // Low price
  close: number; // Close price
  volume: number; // Volume
}
